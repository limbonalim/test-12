import { Typography } from '@mui/material';

const NotFound = () => {
	return (
		<Typography variant="h2" sx={{ textAlign: 'center' }}>
			404 Not Found!
		</Typography>
	);
};

export default NotFound;
